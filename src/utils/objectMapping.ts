/**
 * Object Mapping Utilities
 *
 * Functions for mapping object type IDs to their primary key and name fields.
 * These are re-exported from constants for convenience.
 */

// Re-export from constants
export { getObjectIdFieldName, OBJECT_ID_MAP } from '../constants/objectIds';
export { getNameFieldByObjectType, OBJECT_NAME_MAP } from '../constants/objectNames';
